**DB operations**

- Create DB Controller to interact with DB for CRUD Operations.
- Use DB Controller either in Server Actions, API, or in Server component DB Data fetching.

**DB Interactions through Server Actions and API ROUTE**

- Using Server Actions for Create, Update, Delete.
- Use API Route to fetch the Data from DB and Cache management.

**Showing UI Effects with Server Actions + React Query**

- Using Server Action + React Query to optimal UI update.
- Use server action inside the react query.

**API Route + React Query Integration**

**Data Fectching in Server Component and then Pass it to Client Component**
